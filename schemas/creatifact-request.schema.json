{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/unfallenwill/creatifact/main/schemas/creatifact-request.schema.json",
  "title": "Creatifact request file",
  "description": "Describes one Creatifact command (or a pipeline/parallel run) to run via `creatifact -f <file>.json`. Single-command form: the 'command' field selects the command; the remaining fields map to its arguments, and command-line flags after the file override generate.* fields. Pipeline form: a `pipeline` array runs sequentially, fails fast, and resolves `${name.field}` references to earlier steps' results (tag/digest/outputDir/text/vectors/dimensions/artifacts[N].url). Parallel form: a `parallel` array runs entries as a dependency graph with concurrent independent steps.",
  "$defs": {
    "entry.generate.text2text": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.text2text",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "system": {
          "type": "string",
          "minLength": 1,
          "description": "System prompt (generate.text2text only)."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.text2text": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.text2text",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "system": {
          "type": "string",
          "minLength": 1,
          "description": "System prompt (generate.text2text only)."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.image2text": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.image2text",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "inputs": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Media attachments (generate.image2text / video2text) or texts to embed (generate.embed). Repeatable as --input."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.image2text": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.image2text",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "inputs": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Media attachments (generate.image2text / video2text) or texts to embed (generate.embed). Repeatable as --input."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.video2text": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.video2text",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "inputs": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Media attachments (generate.image2text / video2text) or texts to embed (generate.embed). Repeatable as --input."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.video2text": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.video2text",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "inputs": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Media attachments (generate.image2text / video2text) or texts to embed (generate.embed). Repeatable as --input."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.text2image": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.text2image",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.text2image": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.text2image",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.image2image": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.image2image",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "images": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Reference image(s): http(s)/data URL, local path, or pkg://path into a recipe package. Repeatable as --image. Exactly one for image2image and image2video."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.image2image": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.image2image",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "images": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Reference image(s): http(s)/data URL, local path, or pkg://path into a recipe package. Repeatable as --image. Exactly one for image2image and image2video."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.text2video": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.text2video",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "noWait": {
          "type": "boolean",
          "description": "Video tasks only: submit and print the task handle without polling."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.text2video": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.text2video",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "noWait": {
          "type": "boolean",
          "description": "Video tasks only: submit and print the task handle without polling."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.image2video": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.image2video",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "images": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Reference image(s): http(s)/data URL, local path, or pkg://path into a recipe package. Repeatable as --image. Exactly one for image2image and image2video."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "noWait": {
          "type": "boolean",
          "description": "Video tasks only: submit and print the task handle without polling."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.image2video": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.image2video",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "images": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Reference image(s): http(s)/data URL, local path, or pkg://path into a recipe package. Repeatable as --image. Exactly one for image2image and image2video."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "noWait": {
          "type": "boolean",
          "description": "Video tasks only: submit and print the task handle without polling."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.frames2video": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.frames2video",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "firstFrame": {
          "type": "string",
          "minLength": 1,
          "description": "generate.frames2video only: first frame image."
        },
        "lastFrame": {
          "type": "string",
          "minLength": 1,
          "description": "generate.frames2video only: last frame image."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "noWait": {
          "type": "boolean",
          "description": "Video tasks only: submit and print the task handle without polling."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.frames2video": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.frames2video",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "Text instruction or question (generate.text2text / text2image / image2image / text2video / image2video / frames2video; optional for image2text / video2text)."
        },
        "firstFrame": {
          "type": "string",
          "minLength": 1,
          "description": "generate.frames2video only: first frame image."
        },
        "lastFrame": {
          "type": "string",
          "minLength": 1,
          "description": "generate.frames2video only: last frame image."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "noWait": {
          "type": "boolean",
          "description": "Video tasks only: submit and print the task handle without polling."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "noPack": {
          "type": "boolean",
          "description": "Media tasks only: return artifacts without building a result package."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.embed": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.embed",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "inputs": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Media attachments (generate.image2text / video2text) or texts to embed (generate.embed). Repeatable as --input."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.embed": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.embed",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "inputs": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              }
            }
          ],
          "description": "Media attachments (generate.image2text / video2text) or texts to embed (generate.embed). Repeatable as --input."
        },
        "options": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "Provider-specific options (passed as --opt k=v)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.generate.resume": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.resume",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "handle": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "object",
              "propertyNames": {
                "type": "string"
              },
              "additionalProperties": {}
            }
          ],
          "description": "generate.resume only: the saved task handle (object or inline JSON string)."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.generate.resume": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "generate.resume",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "description": "Model id (overrides any model in 'provider')."
        },
        "timeout": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling timeout, duration string (e.g. \"5m\") or milliseconds."
        },
        "interval": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ],
          "description": "Video tasks and resume: polling interval, duration string (e.g. \"5s\") or milliseconds."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "handle": {
          "anyOf": [
            {
              "type": "string",
              "minLength": 1
            },
            {
              "type": "object",
              "propertyNames": {
                "type": "string"
              },
              "additionalProperties": {}
            }
          ],
          "description": "generate.resume only: the saved task handle (object or inline JSON string)."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.build": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "build",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "dir": {
          "type": "string",
          "minLength": 1,
          "description": "build only: local directory packed as the top layer."
        },
        "file": {
          "type": "string",
          "minLength": 1,
          "description": "Build manifest path (build)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "annotations": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "build only: manifest annotations (values are stringified)."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        },
        "plainHttp": {
          "type": "boolean",
          "description": "Use HTTP instead of HTTPS for the registry."
        }
      },
      "required": [
        "command",
        "tag"
      ],
      "additionalProperties": false
    },
    "single.build": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "build",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Reference name for the result package (default gen-output:latest). Media tasks pack by default; text/embed tasks pack only when tag/output is set. Also build's image reference."
        },
        "dir": {
          "type": "string",
          "minLength": 1,
          "description": "build only: local directory packed as the top layer."
        },
        "file": {
          "type": "string",
          "minLength": 1,
          "description": "Build manifest path (build)."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "annotations": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {},
          "description": "build only: manifest annotations (values are stringified)."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        },
        "plainHttp": {
          "type": "boolean",
          "description": "Use HTTP instead of HTTPS for the registry."
        }
      },
      "required": [
        "command",
        "tag"
      ],
      "additionalProperties": false
    },
    "entry.push": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "push",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "ref": {
          "type": "string",
          "minLength": 1,
          "description": "push / pull only: registry reference, e.g. localhost:5000/myrepo:1.0."
        },
        "layout": {
          "type": "string",
          "minLength": 1,
          "description": "push only: OCI layout directory (default ./oci-layout)."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        },
        "plainHttp": {
          "type": "boolean",
          "description": "Use HTTP instead of HTTPS for the registry."
        }
      },
      "required": [
        "command",
        "ref"
      ],
      "additionalProperties": false
    },
    "single.push": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "push",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "ref": {
          "type": "string",
          "minLength": 1,
          "description": "push / pull only: registry reference, e.g. localhost:5000/myrepo:1.0."
        },
        "layout": {
          "type": "string",
          "minLength": 1,
          "description": "push only: OCI layout directory (default ./oci-layout)."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        },
        "plainHttp": {
          "type": "boolean",
          "description": "Use HTTP instead of HTTPS for the registry."
        }
      },
      "required": [
        "command",
        "ref"
      ],
      "additionalProperties": false
    },
    "entry.pull": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "pull",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "ref": {
          "type": "string",
          "minLength": 1,
          "description": "push / pull only: registry reference, e.g. localhost:5000/myrepo:1.0."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        },
        "plainHttp": {
          "type": "boolean",
          "description": "Use HTTP instead of HTTPS for the registry."
        }
      },
      "required": [
        "command",
        "ref"
      ],
      "additionalProperties": false
    },
    "single.pull": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "pull",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "ref": {
          "type": "string",
          "minLength": 1,
          "description": "push / pull only: registry reference, e.g. localhost:5000/myrepo:1.0."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Result OCI layout directory for packaging tasks (media always; text/embed opt-in with --tag). Resume / --no-pack: directory to save artifacts. Also used by build / pull."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        },
        "plainHttp": {
          "type": "boolean",
          "description": "Use HTTP instead of HTTPS for the registry."
        }
      },
      "required": [
        "command",
        "ref"
      ],
      "additionalProperties": false
    },
    "entry.auth.login": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "auth.login",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "registry": {
          "type": "string",
          "minLength": 1,
          "description": "auth.login / auth.logout only: registry host[:port]."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        }
      },
      "required": [
        "command",
        "registry"
      ],
      "additionalProperties": false
    },
    "single.auth.login": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "auth.login",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "registry": {
          "type": "string",
          "minLength": 1,
          "description": "auth.login / auth.logout only: registry host[:port]."
        },
        "username": {
          "type": "string",
          "minLength": 1,
          "description": "Registry username (package.* / auth.login)."
        },
        "password": {
          "type": "string",
          "minLength": 1,
          "description": "Registry password (package.* / auth.login)."
        }
      },
      "required": [
        "command",
        "registry"
      ],
      "additionalProperties": false
    },
    "entry.auth.logout": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "auth.logout",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "registry": {
          "type": "string",
          "minLength": 1,
          "description": "auth.login / auth.logout only: registry host[:port]."
        }
      },
      "required": [
        "command",
        "registry"
      ],
      "additionalProperties": false
    },
    "single.auth.logout": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "auth.logout",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "registry": {
          "type": "string",
          "minLength": 1,
          "description": "auth.login / auth.logout only: registry host[:port]."
        }
      },
      "required": [
        "command",
        "registry"
      ],
      "additionalProperties": false
    },
    "entry.config.get": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.get",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "key": {
          "type": "string",
          "minLength": 1,
          "description": "config.get / config.set only: dotted config key."
        }
      },
      "required": [
        "command",
        "key"
      ],
      "additionalProperties": false
    },
    "single.config.get": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.get",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "key": {
          "type": "string",
          "minLength": 1,
          "description": "config.get / config.set only: dotted config key."
        }
      },
      "required": [
        "command",
        "key"
      ],
      "additionalProperties": false
    },
    "entry.config.set": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.set",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "key": {
          "type": "string",
          "minLength": 1,
          "description": "config.get / config.set only: dotted config key."
        },
        "value": {
          "description": "config.set only: the value to store (typed JSON).",
          "$ref": "#/$defs/__schema0"
        }
      },
      "required": [
        "command",
        "key",
        "value"
      ],
      "additionalProperties": false,
      "$defs": {
        "__schema0": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            },
            {
              "type": "boolean"
            },
            {
              "type": "null"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/__schema0"
              }
            },
            {
              "type": "object",
              "propertyNames": {
                "type": "string"
              },
              "additionalProperties": {
                "$ref": "#/$defs/__schema0"
              }
            }
          ]
        }
      }
    },
    "single.config.set": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.set",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "key": {
          "type": "string",
          "minLength": 1,
          "description": "config.get / config.set only: dotted config key."
        },
        "value": {
          "description": "config.set only: the value to store (typed JSON).",
          "$ref": "#/$defs/__schema0"
        }
      },
      "required": [
        "command",
        "key",
        "value"
      ],
      "additionalProperties": false,
      "$defs": {
        "__schema0": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            },
            {
              "type": "boolean"
            },
            {
              "type": "null"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/__schema0"
              }
            },
            {
              "type": "object",
              "propertyNames": {
                "type": "string"
              },
              "additionalProperties": {
                "$ref": "#/$defs/__schema0"
              }
            }
          ]
        }
      }
    },
    "entry.models": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "models",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.models": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "models",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider id, optionally with model: 'zhipu' or 'zhipu/cogview-4'. Omit to use the default provider (config key defaults.gen.provider)."
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.config.path": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.path",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.config.path": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.path",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.config.list": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.list",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.config.list": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.list",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "entry.config.reset": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.reset",
          "description": "Which command to run."
        },
        "name": {
          "description": "Step name for ${name.field} references; must be unique.",
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    },
    "single.config.reset": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "const": "config.reset",
          "description": "Which command to run."
        },
        "$schema": {
          "description": "Optional schema reference (ignored).",
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "additionalProperties": false
    }
  },
  "anyOf": [
    {
      "type": "object",
      "properties": {
        "pipeline": {
          "type": "array",
          "minItems": 1,
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/entry.generate.text2text"
              },
              {
                "$ref": "#/$defs/entry.generate.image2text"
              },
              {
                "$ref": "#/$defs/entry.generate.video2text"
              },
              {
                "$ref": "#/$defs/entry.generate.text2image"
              },
              {
                "$ref": "#/$defs/entry.generate.image2image"
              },
              {
                "$ref": "#/$defs/entry.generate.text2video"
              },
              {
                "$ref": "#/$defs/entry.generate.image2video"
              },
              {
                "$ref": "#/$defs/entry.generate.frames2video"
              },
              {
                "$ref": "#/$defs/entry.generate.embed"
              },
              {
                "$ref": "#/$defs/entry.generate.resume"
              },
              {
                "$ref": "#/$defs/entry.build"
              },
              {
                "$ref": "#/$defs/entry.push"
              },
              {
                "$ref": "#/$defs/entry.pull"
              },
              {
                "$ref": "#/$defs/entry.auth.login"
              },
              {
                "$ref": "#/$defs/entry.auth.logout"
              },
              {
                "$ref": "#/$defs/entry.config.get"
              },
              {
                "$ref": "#/$defs/entry.config.set"
              },
              {
                "$ref": "#/$defs/entry.models"
              },
              {
                "$ref": "#/$defs/entry.config.path"
              },
              {
                "$ref": "#/$defs/entry.config.list"
              },
              {
                "$ref": "#/$defs/entry.config.reset"
              }
            ]
          },
          "description": "Pipeline form: entries run sequentially, fail fast, and resolve `${name.field}` references to earlier steps' results."
        },
        "$schema": {
          "type": "string",
          "description": "Optional schema reference (ignored)."
        }
      },
      "required": [
        "pipeline"
      ],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "parallel": {
          "type": "array",
          "minItems": 1,
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/entry.generate.text2text"
              },
              {
                "$ref": "#/$defs/entry.generate.image2text"
              },
              {
                "$ref": "#/$defs/entry.generate.video2text"
              },
              {
                "$ref": "#/$defs/entry.generate.text2image"
              },
              {
                "$ref": "#/$defs/entry.generate.image2image"
              },
              {
                "$ref": "#/$defs/entry.generate.text2video"
              },
              {
                "$ref": "#/$defs/entry.generate.image2video"
              },
              {
                "$ref": "#/$defs/entry.generate.frames2video"
              },
              {
                "$ref": "#/$defs/entry.generate.embed"
              },
              {
                "$ref": "#/$defs/entry.generate.resume"
              },
              {
                "$ref": "#/$defs/entry.build"
              },
              {
                "$ref": "#/$defs/entry.push"
              },
              {
                "$ref": "#/$defs/entry.pull"
              },
              {
                "$ref": "#/$defs/entry.auth.login"
              },
              {
                "$ref": "#/$defs/entry.auth.logout"
              },
              {
                "$ref": "#/$defs/entry.config.get"
              },
              {
                "$ref": "#/$defs/entry.config.set"
              },
              {
                "$ref": "#/$defs/entry.models"
              },
              {
                "$ref": "#/$defs/entry.config.path"
              },
              {
                "$ref": "#/$defs/entry.config.list"
              },
              {
                "$ref": "#/$defs/entry.config.reset"
              }
            ]
          },
          "description": "Parallel form: entries run as a dependency graph — every `${name.field}` reference is a scheduling edge, independent entries run concurrently (width: config defaults.pipeline.concurrency), and a failure skips the not-yet-started."
        },
        "$schema": {
          "type": "string",
          "description": "Optional schema reference (ignored)."
        }
      },
      "required": [
        "parallel"
      ],
      "additionalProperties": false
    },
    {
      "anyOf": [
        {
          "$ref": "#/$defs/single.generate.text2text"
        },
        {
          "$ref": "#/$defs/single.generate.image2text"
        },
        {
          "$ref": "#/$defs/single.generate.video2text"
        },
        {
          "$ref": "#/$defs/single.generate.text2image"
        },
        {
          "$ref": "#/$defs/single.generate.image2image"
        },
        {
          "$ref": "#/$defs/single.generate.text2video"
        },
        {
          "$ref": "#/$defs/single.generate.image2video"
        },
        {
          "$ref": "#/$defs/single.generate.frames2video"
        },
        {
          "$ref": "#/$defs/single.generate.embed"
        },
        {
          "$ref": "#/$defs/single.generate.resume"
        },
        {
          "$ref": "#/$defs/single.build"
        },
        {
          "$ref": "#/$defs/single.push"
        },
        {
          "$ref": "#/$defs/single.pull"
        },
        {
          "$ref": "#/$defs/single.auth.login"
        },
        {
          "$ref": "#/$defs/single.auth.logout"
        },
        {
          "$ref": "#/$defs/single.config.get"
        },
        {
          "$ref": "#/$defs/single.config.set"
        },
        {
          "$ref": "#/$defs/single.models"
        },
        {
          "$ref": "#/$defs/single.config.path"
        },
        {
          "$ref": "#/$defs/single.config.list"
        },
        {
          "$ref": "#/$defs/single.config.reset"
        }
      ]
    }
  ]
}
